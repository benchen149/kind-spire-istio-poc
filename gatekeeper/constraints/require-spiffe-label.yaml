apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sRequiredSpiffeLabel
metadata:
  name: require-spiffe-label
spec:
  match:
    kinds:
      - apiGroups: ["apps"]
        kinds: ["Deployment"]
    namespaceSelector:
      matchLabels:
        spiffe-managed: "true"
